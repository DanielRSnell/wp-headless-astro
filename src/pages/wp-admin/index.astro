---

const { pathname } = Astro.url;
const endpoint = import.meta.env.CORE_URL;
const proxy = `${endpoint}${pathname}`;
console.log(proxy)
const request = await fetch(proxy);
const data = await request.text()

---

<Fragment set:html={data.split(endpoint).join('')} />